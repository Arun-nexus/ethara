{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && chmod +x node_modules/.bin/vite && npm run build"
  },
  "deploy": {
    "startCommand": "npx serve@14 dist -s -p $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}