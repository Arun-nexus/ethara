{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "pip install -r /app/requirements.txt && uvicorn main:app --host 0.0.0.0 --port $PORT --app-dir /app",
    "restartPolicyType": "ON_FAILURE"
  }
}